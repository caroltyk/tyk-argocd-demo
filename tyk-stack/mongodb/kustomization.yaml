resources:
  - ../base/
  - ../tyk-mongo-application.yaml
patches:
  - patch: |-
      kind: Application
      metadata:
        name: tyk-stack
      spec:
        source:
          helm:
            valuesObject:
              global:
                mongo:
                  driver: mongo-go
                  mongoURL: mongodb://mongo-mongodb:27017/tyk_analytics
                storageType: mongo
              tyk-pump:
                pump:
                  backend:
                    - "prometheus"
                    - "mongo"
                  uptimePumpBackend: "mongo"